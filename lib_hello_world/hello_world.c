#include <stdio.h>

int main(void){ // UNIX is free to pass in arguments, but we will ignore them
    printf("Hello, world!\n");    // Rely on C to print
                                  // Fun experiment: check with function
                                  // gets actually assembled
    return 0;
}
